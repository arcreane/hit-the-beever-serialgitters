package com.company;



import java.util.Scanner;

/**
 * @author Rabeb, Mathieu
 */

public class ScoreJoueur {
/*

    //fonction qui enregistre le score

    public static void saveScore(int score){
        String newScore = Integer.toString(score);

        String[][] arrayScore = new String[5][2];
        Scanner scanner = new Scanner(System.in);
        System.out.println(" entrez votre nom ");
        String inputName = scanner.nextLine() ;
        for (int i = 0; i < 5; i++) {
            //System.out.println(arrayScore[inputName][score]);
            arrayScore[i][0] = inputName ;
            arrayScore[i][1] = newScore;

        }

    }

    //trier le tableau de score dans l ordre decroissant

    public static String arrayScore(int score){
        //tri par bulle des scores

        int temp ;
        for (int i = 0; i < 5; i++) {
            for (int j=i+1 ; j<5 ;j++) {
                String[][] arrayScore = new String[5][2];

                if ((arrayScore[1][j]) > (arrayScore[1][i])) {
                    temp = arrayScore[1][j];
                    arrayScore[1][j] = arrayScore[1][i];
                    arrayScore[1][i] = temp;
                }

            }
        }saveScore(score)++ ;
        return arrayScore(score) ;
    }*/
}
